# Rhokell implementation for find_maximum_value_in_array
# This program finds the maximum value in an array.

# Define the function find_maximum_value_in_array
find_maximum_value_in_array = \array ->
    foldl (\acc x -> if x > acc then x else acc) (head array) (tail array)
